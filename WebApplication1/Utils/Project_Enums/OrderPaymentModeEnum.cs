namespace WebApplication1.Utils.Project_Enums
{
    public enum OrderPaymentModeEnum
    {
        Pay_Bnpl = 1,     //Buy now, pay later by installments
        Pay_now_full = 2, //Single, full payment at order time
    }
}